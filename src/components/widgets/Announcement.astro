---
function getDatePlusSeven() {
  const currentDate = new Date();
  const futureDate = new Date(currentDate.getTime() + 7 * 24 * 60 * 60 * 1000);

  const options: Intl.DateTimeFormatOptions = {
    day: 'numeric',
    month: 'long',
  };
  const formattedDate = futureDate.toLocaleDateString('ru-RU', options);

  return `с ${formattedDate}`;
}
---

<div
  class="dark text-muted text-sm bg-black dark:bg-transparent dark:border-b dark:border-slate-800 dark:text-slate-400 hidden md:flex gap-1 overflow-hidden px-3 py-2 relative text-ellipsis whitespace-nowrap"
>
  <span
    class="dark:bg-slate-700 bg-white/40 dark:text-slate-300 font-semibold px-1 py-0.5 text-xs mr-0.5 rtl:mr-0 rtl:ml-0.5 inline-block"
    >Акция</span
  >
  <a href="/pricing" class="text-muted hover:underline dark:text-slate-400 font-medium"
    >Большие скидки {getDatePlusSeven()}</a
  >
  <a
    target="_blank"
    rel="noopener"
    class="ltr:ml-auto rtl:mr-auto w-[5.6rem] h-[1.25rem] px-1 py-0.5 text-xs mr-0.5 bg-contain inline-block dark:bg-slate-700 bg-white/40 dark:text-slate-300 font-semibold"
    title="Позвоните нам"
    href="tel:+7 (922) 807 7855"
    >+7 922 807 7855
  </a>
</div>
